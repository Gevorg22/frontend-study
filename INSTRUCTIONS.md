# 📚 Инструкции по использованию Q&A Hub

## 🎯 Основные возможности

### 1. Просмотр категорий

- Слева в сайдбаре видишь все 10 категорий
- Каждая категория показывает количество вопросов
- Кликни на категорию чтобы открыть список вопросов

### 2. Просмотр вопросов

- На странице категории видишь все вопросы в этом разделе
- Каждый вопрос - это карточка с названием и превью ответа
- Кликни на вопрос чтобы открыть полный ответ

### 3. Страница вопроса

- Видишь полный текст вопроса и ответа
- Кнопка "Показать/Скрыть ответ" для управления видимостью
- Навигация: предыдущий/следующий вопрос в категории
- Прогресс-бар показывает где ты находишься в категории

### 4. Быстрый поиск

- **Нажми Ctrl+K** (или Cmd+K на Mac)
- Введи текст для поиска по названиям вопросов
- Результаты отфильтруются в реальном времени
- Кликни на результат чтобы открыть вопрос
- **Нажми Esc** чтобы закрыть поиск

---

## 📝 Как добавить новые вопросы

### Способ 1: Добавить вручную в JSON

1. Открой файл `questions-data.json` в корне проекта
2. Найди объект `questions` (это массив)
3. Добавь новый объект:

```json
{
  "id": 38,
  "categoryId": "javascript",
  "title": "Название вопроса",
  "answer": "Полный текст ответа"
}
```

4. Сохрани файл
5. Обнови страницу в браузере (F5)

### Способ 2: Импортировать из файлов (если настроишь)

1. Убедись что у тебя есть папка `/questions` с .docx файлами
2. Запусти скрипт:

```powershell
node import-questions.mjs
```

3. Скрипт автоматически:
   - Прочитает все .docx файлы
   - Определит категорию по названию файла
   - Добавит в questions-data.json

---

## 🏷️ Доступные категории

| ID             | Название           | Описание                         |
| -------------- | ------------------ | -------------------------------- |
| `javascript`   | JavaScript         | Основы, прототипы, замыкания     |
| `react`        | React              | Компоненты, хуки, оптимизация    |
| `asynchronous` | Асинхронность      | Promise, async/await, Event Loop |
| `typescript`   | TypeScript         | Типы, интерфейсы, generics       |
| `css`          | CSS                | Flexbox, Grid, селекторы         |
| `http`         | HTTP & API         | Методы, CORS, REST, WebSocket    |
| `performance`  | Производительность | Оптимизация, кэширование         |
| `git`          | Git                | Команды, ветки, конфликты        |
| `testing`      | Тестирование       | Unit тесты, Jest                 |
| `algorithms`   | Алгоритмы          | Сложность, структуры данных      |

---

## 🎨 Структура вопроса

Каждый вопрос имеет свойства:

```typescript
interface Question {
  id: number; // Уникальный ID (автоинкремент)
  categoryId: string; // ID категории (см. таблицу выше)
  title: string; // Названиевопроса
  answer: string; // Полный текст ответа (может содержать переносы)
}
```

### Примеры вопросов

**Хороший вопрос:**

```json
{
  "id": 1,
  "categoryId": "javascript",
  "title": "В чем отличие var, const и let",
  "answer": "var имеет функциональную область видимости и поднимается (hoisting). let и const имеют блочную область видимости. const не позволяет переприсваивать ссылку, но объекты можно мутировать."
}
```

**Еще примеры:**

```json
{
  "id": 2,
  "categoryId": "react",
  "title": "Что такое React.memo",
  "answer": "React.memo - это HOC для мемоизации компонента. Предотвращает переренденирование если пропсы не изменились. Полезно для оптимизации при частых ререндерах родителя."
},
{
  "id": 3,
  "categoryId": "asynchronous",
  "title": "Как работает Event Loop",
  "answer": "Event Loop - это механизм который:\n1. Выполняет синхронный код\n2. Обрабатывает микротаски (Promise)\n3. Обрабатывает макротаски (setTimeout)\n4. Повторяет"
}
```

---

## 🚀 Запуск проекта

```powershell
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для production
npm run build

# Предпросмотр собранного проекта
npm run preview
```

---

## 💡 Советы

1. **Организуй вопросы по разделам** - группируй связанные вопросы в одну категорию
2. **Пиши краткие но полные ответы** - достаточно для понимания, не очень длинные
3. **Используй примеры** - добавляй примеры кода где уместно
4. **Обновляй регулярно** - добавляй новые темы когда учишься

---

## 📱 Адаптивность

Сайт отлично работает на:

- ✅ Десктопе (1024px и выше)
- ✅ Планшете (768px - 1023px)
- ✅ Мобильном (480px - 767px и меньше)

На мобильных:

- Сайдбар сворачивается или преобразуется в меню
- Текст адаптируется для читаемости
- Кнопки увеличены для удобства

---

## 🛠 Технологический стек

- **React 18** - UI библиотека
- **TypeScript** - Статическая типизация
- **Vite** - Быстрый сборщик
- **React Router** - Маршрутизация
- **CSS3** - Стили (Flexbox, Grid, анимации)

---

## 📊 Статистика проекта

- **Компонентов:** 7
- **Категорий:** 10
- **Вопросов:** 37+ (и можно добавлять)
- **Размер бандла:** ~200KB (без gzip)
- **Скорость:** Очень быстро благодаря Vite

---

## ❓ Часто задаваемые вопросы

**Q: Могу ли я экспортировать вопросы?**
A: Да, скопируй содержимое `questions-data.json` - это обычный JSON файл.

**Q: Как добавить новую категорию?**
A: Отредактируй массив `categories` в `questions-data.json`, добавь новый объект категории и обнови ID вопросов.

**Q: Сможет ли это работать офлайн?**
A: Да! Просто скачай сайт и открой index.html в браузере. Данные хранятся локально в JSON.

**Q: Как развернуть в интернете?**
A: Сборка находится в папке `dist`. Её можно загрузить на любой хостинг (GitHub Pages, Vercel, Netlify, и т.д.).

---

Удачи в учебе! 🚀
