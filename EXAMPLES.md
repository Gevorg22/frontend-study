# 📚 Примеры вопросов и ответов для Q&A Hub

Здесь собраны качественные вопросы и ответы которые ты можешь добавить в свой сайт-шпаргалку.

## JavaScript (10+ примеров)

```json
{
  "id": 101,
  "categoryId": "javascript",
  "title": "Что такое замыкание в JavaScript",
  "answer": "Замыкание - это функция, которая имеет доступ к переменным из внешней области видимости (lexical scope), даже после того как внешняя функция завершила выполнение. Создаются благодаря тому, что функции в JavaScript - это объекты первого класса.\n\nПример:\nconst createCounter = () => {\n  let count = 0;\n  return () => ++count; // Замыкание имеет доступ к count\n};"
},
{
  "id": 102,
  "categoryId": "javascript",
  "title": "Как работает this в JavaScript",
  "answer": "this - это ключевое слово которое указывает на объект контекста выполнения функции. Его значение зависит от способа вызова:\n\n1. Обычный вызов: this === window (strict mode: undefined)\n2. Метод объекта: this === объект\n3. new Constructor(): this === новый объект\n4. call/apply/bind: явно указывается\n5. Стрелочная функция: наследует из родительского контекста"
},
{
  "id": 103,
  "categoryId": "javascript",
  "title": "В чем разница null и undefined",
  "answer": "null - это специальное значение, которое явно указывает на отсутствие значения. Это ассигнация разработчика.\nundefined - значение переменной которой не присвоено значение, параметра функции которого не передали, функции без return.\n\nсравнение:\nnull == undefined // true\nnull === undefined // false\ntypeof null // 'object' (это баг в JavaScript)\ntypeof undefined // 'undefined'"
},
{
  "id": 104,
  "categoryId": "javascript",
  "title": "Как работает типизация в JavaScript",
  "answer": "JavaScript использует динамическую типизацию - типы определяются в runtime. Основные типы:\n\nПримитивные: number, string, boolean, null, undefined, symbol, bigint\nОбъектные: Object, Array, Function, Date, RegExp, и др.\n\nПроверка типов:\ntypeof 42 // 'number'\ntypeof 'hello' // 'string'\nArray.isArray([]) // true\n[] instanceof Array // true"
},
{
  "id": 105,
  "categoryId": "javascript",
  "title": "Как работает Event Loop в браузере",
  "answer": "Event Loop - механизм который обрабатывает код, события и обновляет UI:\n\n1. Выполняется весь синхронный код\n2. Обрабатываются все микротаски (Promise.then, async/await, queueMicrotask)\n3. Рендер (если нужен)\n4. Обрабатывается одна макротаска (setTimeout, setInterval, requestAnimationFrame)\n5. Повторить с пункта 2\n\nМикротаски имеют БОЛЬШИЙ приоритет чем макротаски!"
}
```

## React (10+ примеров)

```json
{
  "id": 201,
  "categoryId": "react",
  "title": "Как работает хук useState",
  "answer": "useState - хук который добавляет локальное состояние функциональному компоненту.\n\nСинтаксис:\nconst [value, setValue] = useState(initialValue);\n\n- value: текущее значение состояния\n- setValue: функция для обновления состояния\n- initialValue: начальное значение (или функция для ленивой инициализации)\n\nПри изменении состояния компонент переренденивается.\nЛучше передавать функцию initializer если начальное значение дорогое для вычисления."
},
{
  "id": 202,
  "categoryId": "react",
  "title": "Когда использовать useCallback",
  "answer": "useCallback мемоизирует функцию и возвращает ту же ссылку пока не изменятся зависимости.\n\nКогда нужен:\n1. Передаешь функцию как пропс в оптимизированный компонент (React.memo)\n2. Функция используется в зависимостях других хуков\n\nКогда не нужен:\n1. Если функция не передается как пропс\n2. Если дочерние компоненты не мемоизированы\n\nПримечание: В React 19 с новым компилятором useCallback может вообще не понадобиться."
},
{
  "id": 203,
  "categoryId": "react",
  "title": "Как работает useEffect",
  "answer": "useEffect запускает побочные эффекты после рендера компонента.\n\nСинтаксис:\nuseEffect(() => {\n  // код эффекта\n  return () => {\n    // очистка (опционально)\n  };\n}, [dependencies]);\n\n- [] - эффект запускается 1 раз после монтирования\n- [dep1, dep2] - эффект запускается когда изменяются зависимости\n- no deps - эффект запускается после каждого рендера\n\nОчистка вызывается перед демонтированием или перед новым эффектом."
},
{
  "id": 204,
  "categoryId": "react",
  "title": "В чем разница классовых и функциональных компонентов",
  "answer": "Функциональные компоненты - это функции возвращающие JSX.\nКлассовые компоненты - это классы наследующие React.Component.\n\nОтличия:\n\nФункциональные:\n✅ Проще писать и читать\n✅ Меньше кода благодаря хукам\n✅ Легче тестировать\n✅ Лучше для FP\n\nКлассовые:\n❌ Больше кода\n✅ Lifecycle методы явные\n✅ Error Boundary (пока только для классов)\n\nСегодня функциональные - стандарт благодаря хукам."
}
```

## TypeScript (10+ примеров)

```json
{
  "id": 301,
  "categoryId": "typescript",
  "title": "В чем разница Type и Interface",
  "answer": "Оба используются для описания типов объектов, но с различиями:\n\nInterface:\n- Только для объектов\n- Можно расширять (extends)\n- Можно объединять (declaration merging)\n- Явная структура\n\nType:\n- Для объектов и всего остального\n- Можно использовать union (Type1 | Type2)\n- Можно использовать intersection (&)\n- Более гибкий\n\nПрактика: Используй Interface для объектов, Type для всего остального."
},
{
  "id": 302,
  "categoryId": "typescript",
  "title": "Что такое generics в TypeScript",
  "answer": "Generics позволяют создавать переиспользуемые компоненты которые работают с разными типами.\n\nПримеры:\n\nФункция:\nfunction identity<T>(arg: T): T { return arg; }\n\nИнтерфейс:\ninterface Box<T> { content: T; }\n\nКласс:\nclass Container<T> { item: T; }\n\nОграничения (constraints):\nfunction getLength<T extends { length: number }>(item: T) {\n  return item.length;\n}\n\nGenerics делают код типобезопасным и переиспользуемым."
},
{
  "id": 303,
  "categoryId": "typescript",
  "title": "Что такое utility types",
  "answer": "Utility types - встроенные типы для трансформации других типов:\n\nПопулярные:\n- Partial<T> - все свойства опциональны\n- Required<T> - все свойства обязательны\n- Readonly<T> - все свойства только для чтения\n- Pick<T, K> - выбрать свойства K из T\n- Omit<T, K> - исключить свойства K из T\n- Record<K, T> - объект с ключами K и значениями T\n- Exclude<T, U> - исключить типы U из T\n- Extract<T, U> - оставить только типы U из T\n- NonNullable<T> - исключить null и undefined"
}
```

## CSS (10+ примеров)

```json
{
  "id": 401,
  "categoryId": "css",
  "title": "Как работает Flexbox",
  "answer": "Flexbox - одномерная система для раскладки элементов в строки или столбцы.\n\nКонтейнер (display: flex):\n- flex-direction: row (по умолчанию), column\n- justify-content: выравнивание по основной оси\n- align-items: выравнивание по поперечной оси\n- gap: расстояние между элементами\n- flex-wrap: wrap для переноса на новую строку\n\nЭлементы (flex children):\n- flex: 1 - занимает свободное место\n- flex-grow, flex-shrink, flex-basis\n- align-self: выравнивание одного элемента\n\nУ гибкая и мощная система для адаптивных макетов."
},
{
  "id": 402,
  "categoryId": "css",
  "title": "Как работает CSS Grid",
  "answer": "CSS Grid - двумерная система для раскладки элементов в сетку.\n\nКонтейнер (display: grid):\n- grid-template-columns: 1fr 2fr 1fr\n- grid-template-rows: 100px auto 50px\n- grid-gap: расстояние между ячейками\n- grid-auto-flow: row (по умолчанию) или column\n- grid-template-areas: именованные области\n\nЭлементы:\n- grid-column: 1 / 3 (займет 2 колонки)\n- grid-row: 1 / 4 (займет 3 ряда)\n- grid-area: именная область\n\nGrid идеален для сложных макетов с четкой структурой."
},
{
  "id": 403,
  "categoryId": "css",
  "title": "Как работает CSS специфичность",
  "answer": "Специфичность определяет какой стиль применяется при конфликте.\n\nПорядок (от меньшего к большему):\n1. Элемент (1)\n2. Класс / псевдокласс (10)\n3. ID (100)\n4. Inline стили (1000)\n5. !important (наивысший приоритет)\n\nПримеры:\np { } - специфичность 1\n.class { } - специфичность 10\n#id { } - специфичность 100\nstyle=\"color: red;\" - специфичность 1000\n\nСовет: Избегай !important и ID в селекторах. Используй классы."
}
```

## HTTP & API (10+ примеров)

```json
{
  "id": 501,
  "categoryId": "http",
  "title": "Что такое CORS и как с ним работать",
  "answer": "CORS (Cross-Origin Resource Sharing) - механизм который контролирует доступ к ресурсам с других доменов.\n\nПроблема: браузер блокирует запросы с других origin по умолчанию.\n\nРешение на сервере (headers):\nAccess-Control-Allow-Origin: https://example.com\nAccess-Control-Allow-Methods: GET, POST, PUT\nAccess-Control-Allow-Headers: Content-Type\nAccess-Control-Allow-Credentials: true\n\nPreflight запрос: браузер делает OPTIONS запрос перед реальным запросом.\n\nОбход:\n- JSONP (старый способ)\n- Proxy сервер\n- Правильная настройка CORS"
},
{
  "id": 502,
  "categoryId": "http",
  "title": "Разница GET и POST запроса",
  "answer": "GET:\n- Для получения данных\n- Параметры в URL (?key=value)\n- Не должен иметь body\n- Кэшируется браузером\n- Видимы в истории\n- Ограничение по размеру URL\n- Идемпотентен\n\nPOST:\n- Для отправки данных на сервер\n- Параметры в body (JSON, form-data)\n- Может иметь body\n- Не кэшируется по умолчанию\n- Не видны в истории\n- Нет ограничений по размеру\n- Не идемпотентен\n\nВыбирай GET для получения, POST для создания/изменения."
},
{
  "id": 503,
  "categoryId": "http",
  "title": "Что такое REST API",
  "answer": "REST (Representational State Transfer) - архитектурный стиль для веб-сервисов.\n\nПринципы:\n1. Клиент-сервер архитектура\n2. Stateless - каждый запрос содержит всю нужную информацию\n3. Ресурсы - все данные представлены как ресурсы с URI\n4. Методы HTTP - операции над ресурсами\n\nОперации (CRUD):\n- GET /users - получить всех пользователей\n- POST /users - создать пользователя\n- GET /users/1 - получить пользователя с ID 1\n- PUT /users/1 - заменить пользователя с ID 1\n- PATCH /users/1 - обновить частично\n- DELETE /users/1 - удалить пользователя\n\nВсегда используй правильные методы для операций."
}
```

---

## Как добавить эти примеры

1. Открой файл `questions-data.json`
2. Найди массив `questions`
3. Скопируй нужные примеры
4. Убедись что ID уникальные
5. Сохрани файл
6. Обнови страницу в браузере

**Совет:** Меняй ID на более высокие чтобы избежать конфликтов с существующими вопросами.

---

Успехов в создании своей шпаргалки! 📚🚀
