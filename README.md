# Q&A Hub - Шпаргалка для фронтенд разработчиков

Красивый и функциональный сайт-шпаргалка с вопросами и ответами по фронтенд разработке.

## 🚀 Возможности

- ✅ **Левое меню** с категориями (JavaScript, React, TypeScript, CSS, HTTP, Git и др.)
- ✅ **Список вопросов** в каждой категории
- ✅ **Детальная страница** с полным ответом на вопрос
- ✅ **Быстрый поиск** (Ctrl+K или Cmd+K)
- ✅ **Навигация** между вопросами (предыдущий/следующий)
- ✅ **Прогресс** по категориям
- ✅ **Адаптивный дизайн** для мобильных устройств
- ✅ **Красивый UI** с градиентом и плавными переходами

## 📋 Структура проекта

```
src/
├── components/
│   ├── Sidebar.tsx              # Левое меню с категориями
│   ├── Home.tsx                 # Главная страница
│   ├── CategoryPage.tsx          # Страница категории со списком вопросов
│   ├── QuestionDetailPage.tsx    # Страница вопроса и ответа
│   ├── QuestionItem.tsx          # Карточка вопроса в списке
│   ├── CategoryCard.tsx          # Карточка категории на главной
│   ├── SearchResults.tsx         # Модал поиска
│   └── *.css                     # Стили компонентов
├── context.tsx                   # React Context для данных
├── types.ts                      # TypeScript типы
├── App.tsx                       # Главное приложение
└── main.tsx                      # Entry point

questions-data.json              # Данные с вопросами и ответами
```

## 🛠 Установка и запуск

### Требования

- Node.js 16+
- npm или yarn

### Установка

```powershell
# Перейти в папку проекта
cd "path/to/frontend-study"

# Установить зависимости
npm install
```

### Запуск в режиме разработки

```powershell
npm run dev
```

Сайт откроется на `http://localhost:5173/`

### Сборка для продакшена

```powershell
npm run build
```

## ⌨️ Горячие клавиши

- **Ctrl+K** (или **Cmd+K** на Mac) - открыть поиск
- **Esc** - закрыть поиск

## 📝 Добавление новых вопросов

Вопросы хранятся в файле `questions-data.json`. Добавьте новый объект в массив `questions`:

```json
{
  "id": 38,
  "categoryId": "javascript",
  "title": "Ваш вопрос здесь",
  "answer": "Полный ответ на вопрос..."
}
```

### Доступные категории (categoryId):

- `javascript` - JavaScript
- `react` - React
- `asynchronous` - Асинхронность
- `typescript` - TypeScript
- `css` - CSS
- `http` - HTTP & API
- `performance` - Производительность
- `git` - Git
- `testing` - Тестирование
- `algorithms` - Алгоритмы & структуры

## 🎨 Технологии

- **React 18** - UI библиотека
- **TypeScript** - Статическая типизация
- **Vite** - Сборщик проекта
- **React Router** - Маршрутизация
- **CSS3** - Стилизация (Flexbox, Grid, градиенты)

## 📱 Адаптивность

Сайт полностью адаптирован для:

- 🖥️ Десктопа (1024px+)
- 📱 Планшета (768px-1023px)
- 📲 Мобильного (480px-767px)

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано как шпаргалка для фронтенд разработки

---

**Совет:** Используй Ctrl+K для быстрого поиска по вопросам!

Build for production:

```powershell
npm run build
npm run preview
```

Notes:

- Uses Vite and React 18.
- TypeScript config is strict by default.
